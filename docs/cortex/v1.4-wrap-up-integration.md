# v1.4 /wrap-up Integration Spec

**Last Updated**: 2025-12-08  
**Status**: Phase 4 - Ready for Implementation  
**Owner**: Cortex OS  
**Prerequisite**: Phase 3 (detect-incomplete-tasks.py) ✅ Complete

---

## 1. Purpose / Goal

### 1.1 目的

- `/wrap-up` コマンドに **Phase 3 の未完了タスク検出** を統合する
- 1日の終了時に、以下を **1コマンドで完結** させる：
  1. 今日の完了タスク確定
  2. 未完了タスクの自動検出 & carryover 生成
  3. 完了率の自動算出 & digest への反映
  4. 翌日の候補タスク生成
  5. task-entry.json の更新

### 1.2 非ゴール

- TODO.md の構造変更（既存の `/wrap-up` の責務を維持）
- digest の構造変更（append-only policy を維持）
- 完了率の閾値調整・自動判断（v1.4 ではデータ記録のみ）

---

## 2. Current State Analysis

### 2.1 既存 /wrap-up の責務

**データ源**:
- `notifications/daily/YYYY-MM-DD-digest.md` or `cortex/daily/YYYY-MM-DD-digest.md`
- `TODO.md`

**処理フロー**:
1. Digest から `[x] task` 抽出
2. TODO.md 内の対応タスクを `[ ]` → `[x]` に更新
3. 完了タスクを `## Archive` へ移動
4. 未完了タスクを翌日へ引き継ぎ
5. Reflection 抽出 & 構造化フィードバック追加
6. `cortex/state/tomorrow.json` 生成

**構造化フィードバック（v1.3+）**:
```markdown
---
**Mood**: 😀 / 🙂 / 😐 / 🙁 / 😞  
**Energy**: 7/10  
**Satisfaction**: 8/10  
```

### 2.2 Phase 3 で追加された機能

**新規スクリプト**: `scripts/detect-incomplete-tasks.py`

**機能**:
- `task-entry-YYYY-MM-DD.json` から未完了タスクを自動検出
- `carryover` に未完了タスクを書き込み
- `metadata.incomplete_detection` に完了率統計を記録

**出力例**:
```json
{
  "metadata": {
    "incomplete_detection": {
      "version": "1.0.0",
      "detected_at": "2025-12-08T09:43:50.788930+00:00",
      "planned": 5,
      "completed": 3,
      "incomplete": 2,
      "completion_rate": 0.6
    }
  },
  "carryover": [
    {"content": "未完了タスク1", "category": "core-work"},
    {"content": "未完了タスク2", "category": "learning"}
  ]
}
```

---

## 3. Integration Design

### 3.1 統合方針

**Phase 3 との連携**:
- `/wrap-up` の処理フローに `detect-incomplete-tasks` を組み込む
- digest への反映は **軽量・自然** にする（append-only policy 維持）
- 既存の `/wrap-up` の出力形式を維持しつつ、完了率セクションを追加

**データフロー**:
```
/wrap-up 実行
  ↓
1. Digest 読み込み
  ↓
2. sync-digest-tasks.py 実行（digest → task-entry 同期）
  ↓
3. detect-incomplete-tasks.py 実行（完了率算出 & carryover 生成）
  ↓
4. 完了率を digest の Reflection セクションに追記
  ↓
5. tomorrow.json 生成（carryover を tomorrow_candidates に含める）
  ↓
6. TODO.md 更新
```

### 3.2 実装レイヤー

**Option A: Python wrapper 新規作成（推奨）**
- 新規: `scripts/wrap-up.py`
- 既存スクリプトを orchestrate:
  - `sync-digest-tasks.py`
  - `detect-incomplete-tasks.py`
  - TODO.md 更新ロジック（新規実装）
- 利点: テスト容易、再利用可能、段階的実装

**Option B: Claude の /wrap-up コマンド内で直接呼び出し**
- `.claude/commands/wrap-up.md` から Python スクリプトを呼び出す
- 利点: 既存フロー維持、ユーザー体験変更なし
- 欠点: テストしづらい、自動化しづらい

**→ v1.4 では Option A を採用**（テスト可能性・自動化を優先）

---

## 4. Implementation Spec

### 4.1 新規スクリプト: `scripts/wrap-up.py`

**責務**:
- 1日の終了処理を orchestrate
- 既存スクリプトを呼び出し、データを統合
- digest への完了率追記
- tomorrow.json 生成

**入力**:
- 引数: `date` (省略時は JST 今日)

**出力**:
- 標準出力: wrap-up サマリー（既存の `/wrap-up` 出力と互換）
- ファイル更新:
  - `cortex/state/task-entry-YYYY-MM-DD.json`
  - `cortex/state/tomorrow.json`
  - `cortex/daily/YYYY-MM-DD-digest.md` (Reflection セクション)

### 4.2 処理フロー（詳細）

```python
def wrap_up(date: str):
    """
    1日の終了処理
    
    Args:
        date: YYYY-MM-DD format
    """
    # Step 1: Paths
    digest_path = f"cortex/daily/{date}-digest.md"
    task_entry_path = f"cortex/state/task-entry-{date}.json"
    tomorrow_path = "cortex/state/tomorrow.json"
    
    # Step 2: Sync digest → task-entry
    print("🔄 Syncing digest to task-entry...")
    sync_result = subprocess.run([
        "python", "scripts/sync-digest-tasks.py", date
    ], capture_output=True, text=True)
    
    if sync_result.returncode != 0:
        print(f"ERROR: sync-digest-tasks failed: {sync_result.stderr}")
        sys.exit(1)
    
    # Step 3: Detect incomplete tasks
    print("🔎 Detecting incomplete tasks...")
    detect_result = subprocess.run([
        "python", "scripts/detect-incomplete-tasks.py", date
    ], capture_output=True, text=True)
    
    if detect_result.returncode != 0:
        print(f"ERROR: detect-incomplete-tasks failed: {detect_result.stderr}")
        sys.exit(1)
    
    # Step 4: Load updated task-entry
    with open(task_entry_path, "r") as f:
        task_entry = json.load(f)
    
    detection = task_entry["metadata"]["incomplete_detection"]
    carryover = task_entry.get("carryover", [])
    
    # Step 5: Append completion rate to digest Reflection section
    append_completion_rate_to_digest(digest_path, detection)
    
    # Step 6: Generate tomorrow.json
    generate_tomorrow_json(date, carryover, detection, tomorrow_path)
    
    # Step 7: Display summary
    display_wrap_up_summary(date, detection, carryover)
```

### 4.3 digest への完了率追記

**挿入位置**: `## 振り返り` セクションの最後

**フォーマット**:
```markdown
## 振り返り

（既存の振り返り内容）

---
**Mood**: 😀  
**Energy**: 7/10  
**Satisfaction**: 8/10  

---
**📊 Today's Metrics** (v1.4)  
**Planned**: 5 | **Completed**: 3 | **Rate**: 60% 🟡
```

**実装**:
```python
def append_completion_rate_to_digest(digest_path: str, detection: dict):
    """
    digest の Reflection セクションに完了率を追記
    """
    with open(digest_path, "r") as f:
        digest_content = f.read()
    
    # Find Reflection section
    if "## 振り返り" not in digest_content:
        print("WARN: No Reflection section found, skipping append")
        return
    
    # Generate metrics line
    planned = detection["planned"]
    completed = detection["completed"]
    rate = detection["completion_rate"]
    rate_pct = int(rate * 100)
    
    # Determine emoji
    if rate >= 0.8:
        emoji = "🟢"
    elif rate >= 0.5:
        emoji = "🟡"
    else:
        emoji = "🔴"
    
    metrics_line = (
        f"\n---\n"
        f"**📊 Today's Metrics** (v1.4)  \n"
        f"**Planned**: {planned} | **Completed**: {completed} | **Rate**: {rate_pct}% {emoji}\n"
    )
    
    # Append to end of file (assume Reflection is last section)
    updated_content = digest_content.rstrip() + "\n" + metrics_line
    
    with open(digest_path, "w") as f:
        f.write(updated_content)
    
    print(f"✅ Appended completion rate to {digest_path}")
```

### 4.4 tomorrow.json 生成

**フォーマット**:
```json
{
  "generated_at": "2025-12-08T13:00:00+00:00",
  "source_date": "2025-12-08",
  "tomorrow_candidates": [
    "未完了タスク1",
    "未完了タスク2",
    "推奨タスク1"
  ],
  "carryover_tasks": [
    "未完了タスク1",
    "未完了タスク2"
  ],
  "reflection_summary": "今日の気づきを1行で",
  "completion_rate": 0.6,
  "metrics": {
    "planned": 5,
    "completed": 3,
    "incomplete": 2
  }
}
```

**実装**:
```python
def generate_tomorrow_json(date: str, carryover: list, detection: dict, output_path: str):
    """
    tomorrow.json を生成
    """
    # Extract carryover titles
    carryover_titles = [task["content"] for task in carryover if "content" in task]
    
    # TODO: 将来的に reflection_summary を digest から抽出
    reflection_summary = "（今後実装）"
    
    tomorrow_data = {
        "generated_at": datetime.now(timezone.utc).isoformat(),
        "source_date": date,
        "tomorrow_candidates": carryover_titles[:3],  # Top 3
        "carryover_tasks": carryover_titles,
        "reflection_summary": reflection_summary,
        "completion_rate": detection["completion_rate"],
        "metrics": {
            "planned": detection["planned"],
            "completed": detection["completed"],
            "incomplete": detection["incomplete"]
        }
    }
    
    with open(output_path, "w") as f:
        json.dump(tomorrow_data, f, ensure_ascii=False, indent=2)
    
    print(f"✅ Generated {output_path}")
```

### 4.5 出力サマリー

**標準出力**（既存の `/wrap-up` 形式を維持）:
```text
## Daily Wrap-up — 2025-12-08

### 📊 Today's Metrics

| 項目 | 値 |
|------|-----|
| 計画タスク | 5 |
| 完了タスク | 3 |
| 完了率 | 60% |
| 判定 | 🟡 標準ペース |
| 持ち越しタスク | 2 件 |

---

### 🧾 未完了タスク

- [ ] 未完了タスク1
- [ ] 未完了タスク2

---

### 🌅 Tomorrow candidates

```json
{
  "next_candidates": [
    "未完了タスク1",
    "未完了タスク2"
  ]
}
```

---

### ✅ Updated Files

- ✅ cortex/state/task-entry-2025-12-08.json
- ✅ cortex/daily/2025-12-08-digest.md
- ✅ cortex/state/tomorrow.json
```

---

## 5. Error Handling

### 5.1 異常系

**sync-digest-tasks.py が失敗**:
- wrap-up を中断（safe-fail）
- エラーメッセージを表示
- ユーザーに手動確認を促す

**detect-incomplete-tasks.py が失敗**:
- wrap-up を中断（safe-fail）
- エラーメッセージを表示

**digest が存在しない**:
- WARN ログを出力
- task-entry のみで処理を続行
- tomorrow.json は生成するが、reflection_summary は空

### 5.2 ログレベル

既存の v1.4 ポリシーに従う:
- `INFO`: 正常処理
- `WARN`: 軽微な問題（処理続行可能）
- `ERROR`: 致命的問題（処理中断）

---

## 6. Testing

### 6.1 テストファイル

- `tests/scripts/test_wrap_up.py`（新規作成）

### 6.2 テストケース

1. **正常系: 完了率 60%**
   - digest あり、task-entry あり
   - carryover 生成、tomorrow.json 生成

2. **完了率 100%**
   - carryover なし
   - tomorrow.json には carryover なし（推奨タスクのみ）

3. **完了率 0%**
   - 全タスクが carryover
   - tomorrow.json に全タスクが含まれる

4. **digest なし**
   - task-entry のみで処理
   - reflection_summary は空

5. **sync-digest-tasks 失敗**
   - wrap-up が中断される
   - エラーメッセージが表示される

---

## 7. CLI Interface

### 7.1 実行方法

```bash
# 今日の wrap-up
python scripts/wrap-up.py

# 特定日付の wrap-up
python scripts/wrap-up.py 2025-12-08
```

### 7.2 推奨 alias

```bash
# .zshrc or .bashrc
alias wrap-up='python /path/to/scripts/wrap-up.py'
```

---

## 8. Integration with Claude /wrap-up

### 8.1 段階的移行

**Phase 4.1**: Python スクリプト完成 & テスト（今回）
**Phase 4.2**: Claude の `/wrap-up` から `wrap-up.py` を呼び出す

**Phase 4.2 の実装イメージ**:
```markdown
<!-- .claude/commands/wrap-up.md -->

## 実行フロー

1. Python スクリプトを実行:
   ```bash
   python scripts/wrap-up.py
   ```

2. 出力を解析し、ユーザーに提示

3. TODO.md の更新案を生成（既存ロジック）

4. ユーザーの承認後、TODO.md を更新
```

### 8.2 TODO.md 更新

**v1.4 では TODO.md 更新は既存の `/wrap-up` に委譲**:
- Python スクリプトは task-entry / digest / tomorrow.json のみ更新
- TODO.md の更新は Claude の `/wrap-up` が担当（既存ロジック維持）

---

## 9. Future Extensions (v1.5+)

### 9.1 週次まとめ（Weekly Intelligence）

- 7日分の `incomplete_detection` を集計
- 週次完了率トレンド
- 慢性的 carryover タスクの検出

### 9.2 自動 rollover

- tomorrow.json の carryover を翌日の digest に自動転記
- Nightly Recipe or Morning Brief で実行

### 9.3 Reflection summary 自動抽出

- digest の Reflection セクションから1行サマリーを LLM で生成
- tomorrow.json に含める

---

## 10. Dependencies

### 10.1 必須

- ✅ Phase 1: `/log` `/note` コマンド
- ✅ Phase 2: `sync-digest-tasks.py`
- ✅ Phase 3: `detect-incomplete-tasks.py`

### 10.2 オプション

- Obsidian MCP（digest 更新用）
- n8n Recipe（夜間自動実行用）

---

**Status**: Spec Complete - Ready for Implementation 🚀  
**Next**: `scripts/wrap-up.py` + tests
