/**
 * Legacy TS test file kept to avoid breaking imports; actual runnable tests moved to booking.test.js
 */
describe.skip("Booking API contract (TS legacy)", () => {
  it("migrated to JS", () => {
    expect(true).toBe(true);
  });
});
