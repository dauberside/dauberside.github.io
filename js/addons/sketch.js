var canvas;

function setup() {
  // put setup code here
  canvas = createCanvas(windowWidth, windHeight);
  canvas.position(0, 0);
  canvas.style('z-index', '-1');
}

function draw() {
  // put drawing code here
  background(175);
}
