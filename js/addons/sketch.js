var canvas;

function setup() {
  // put setup code here
  createCanvas(400, 400);
  canvas.position(0, 0);
  canvas.style('z-index', '-1');
}

function draw() {
  // put drawing code here
  background(0);
}
